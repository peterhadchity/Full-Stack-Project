/* config.js — holds the API Ninjas key used by nutrition-api.js.
   Note: on a static front-end the key is visible in the browser; this is
   inherent to client-side API calls. The free key is rate-limited and can
   be regenerated anytime from the API Ninjas dashboard. */
const CONFIG = {
  NINJAS_KEY: "m64hZw1vCAl76lGheyixTxBeihorydzWD1W46VDN",
};
